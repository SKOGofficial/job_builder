[pytest]
testpaths = tests
asyncio_mode = auto

# NiceGUI's user fixture imports a "main file" to register pages. This project
# registers them from tests/conftest.py instead, so there is nothing to import.
main_file =
