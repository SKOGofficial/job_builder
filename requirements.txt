# UI
nicegui
# Optional: gives a native window instead of a browser tab. The app falls back
# to the browser when this is absent.
pywebview

# Gmail
google-api-python-client
google-auth
google-auth-oauthlib

# Research and resume generation. Claude does the low-volume, high-value half
# of the work (company research with server-side web search, then tailoring),
# while Groq below does the high-volume classification. The app runs without
# this installed - research is skipped and leads simply wait unprepared.
anthropic

# Shared: Groq and Gemini both talk over plain REST, so neither needs a provider
# SDK - one HTTP call with a JSON body does not justify a dependency, and the
# injectable poster is what keeps the tests off the network.
python-dotenv
keyring
requests
